# syntax=docker/dockerfile:1.8
FROM gcr.io/distroless/static:nonroot

# Copy binary with execute bit set
COPY --chmod=0755 deploy/docker/artifacts/auth-service /auth-service

EXPOSE 50051
USER nonroot
ENTRYPOINT ["/auth-service"]