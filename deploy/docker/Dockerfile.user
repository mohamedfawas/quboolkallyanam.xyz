# syntax=docker/dockerfile:1.8
FROM gcr.io/distroless/static:nonroot

# Copy the compiled binary from CI artifacts into the container
COPY --chmod=0755 deploy/docker/artifacts/user-service /user-service

EXPOSE 50052
USER nonroot

ENTRYPOINT ["/user-service"]
